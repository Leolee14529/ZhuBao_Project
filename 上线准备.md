# ZhuBao 上线准备状态

更新时间：2026-06-27

## 已完成

- 五行计算、保存、读取均要求登录。
- 后端从 token 推导用户身份，不接受客户端指定 `userId`。
- 新增退出登录和服务端 session 撤销。
- 新增 PostgreSQL 表结构、迁移脚本和 Repository 适配。
- 生产环境缺少数据库或微信密钥时拒绝启动。
- 新增统一 API 响应、JSON 404、请求 ID、登录限流和安全响应头。
- 新增存活与就绪健康检查。
- 前端五行保存、读取和个人中心接入真实后端。
- 所有 JS、WXML、WXSS 单文件不超过 300 行。
- 后端自动化测试覆盖认证、越权防护、五行算法、周期算法和前端结果转换。

## 上线前必须补充

1. 正式 HTTPS API 域名。
2. 微信公众平台请求域名配置。
3. 正式 `WECHAT_APPID` 和 `WECHAT_SECRET`，通过部署平台密钥注入。
4. 生产 PostgreSQL 地址、最小权限账号、自动备份和恢复演练。
5. 隐私政策、出生信息和周期数据的保留/删除规则。
6. 微信开发者工具真机登录与五行完整回归。
7. 服务器日志、错误告警和可用性监控接入。

## 发布闸门

发布前以下命令必须全部通过：

```bash
cd backend
pnpm install --frozen-lockfile
pnpm run check:syntax
pnpm test
pnpm run check:lines
pnpm audit --prod
DATABASE_URL=... pnpm run db:migrate
```

发布验证：

- `/health/live` 返回 200。
- `/health/ready` 返回 200 且数据库健康。
- 未登录访问五行接口返回 401。
- 用户不能读取或覆盖其他用户结果。
- 退出后旧 token 返回 401。
- 小程序 release 版本使用正式 HTTPS 域名，不允许回退 localhost。

## Git 注意事项

当前工作区包含施工前已有的未提交修改。发布前必须人工确认差异来源，禁止直接覆盖、清理或强推；`project.private.config.json` 不应作为生产配置来源。
