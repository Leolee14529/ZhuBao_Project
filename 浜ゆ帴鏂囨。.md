# 珠宝小程序项目交接文档

> 状态更新（2026-07-08）：本文档中的“本地 JSON 后端、无真实鉴权”描述属于初始交接状态。当前代码已经加入微信登录、用户 session、五行用户绑定、PostgreSQL 迁移、统一响应和自动化测试。小程序提审入口已调整为主包 `pages/home/index`，产品口径收敛为“心情记录与珠宝风格参考工具”。后端现状与启动方式以 `backend/README.md` 为准。

## 1. 项目概述

本项目是一个微信小程序，主题方向为“珠宝风格参考 + 五行色彩 + 设备联动 + 周期记录”。

当前代码分为两部分：

- 小程序前端：`D:\st\app\珠宝\新建文件夹`
- 本地 Node.js 后端：`D:\st\app\珠宝\新建文件夹\backend`

前端目前更像是一个原型和演示版集合，已经具备多套页面和交互骨架；后端已实现五行测算相关接口，并支持保存最近一次测算结果。

## 2. 目录说明

### 前端主目录

- `app.js`
  - 小程序入口，主要处理全局导航栏安全区信息。
- `app.json`
  - 页面注册与分包配置。
- `pages/home`
  - 主包首页，支持匿名浏览，展示今日灵感、珠宝风格入口和基础设备状态。
- `pages/login`
  - 登录页，用户需要保存个人资料或进入受限功能时再进入。
- `components/empty-state`
  - 通用空状态组件。

### 分包目录

- `subpackage/jewelry`
  - 主珠宝业务分包。
  - 包含数据页、设置页、五行定制页。旧分包首页已迁移到主包首页。
- `subpackage/auth`
  - 注册/认证相关页面。
  - 目前有邮箱页、密码页及若干表单组件。
- `subpackage/device12`
  - 设备已连接页。
- `subpackage/device13`
  - 设备配对中页。
- `subpackage/device17`
  - 设备未连接详情页。
- `subpackage/periodCalendar`
  - 周期日历功能。
  - 支持周期设置、日历展示、参考日期调整。
- `subpackage/showcase`
  - 首页展示版。
- `subpackage/showcase-device`
  - 设备联动展示版。

### 后端目录

- `backend/server.js`
  - Express 服务入口，默认端口 `3000`。
- `backend/routes/wuxing.js`
  - 五行接口路由。
- `backend/services/baziService.js`
  - 八字/五行核心计算逻辑。
- `backend/data/wuxing.json`
  - 最近一次结果的本地存储文件。
- `backend/package.json`
  - 后端依赖与启动脚本。

## 3. 当前页面与功能状态

### 已有内容

- 登录页已完成基础 UI 和跳转逻辑。
- 主包首页已完成：
  - 今日灵感卡展示
  - 产品卡片展示
  - 设备入口跳转
  - 五行定制入口跳转
- 五行定制页已完成本地保存出生日期、出生时间，并生成预览文案。
- 周期日历页已完成：
  - 周期参数录入
  - 月历展示
  - 周期记录/参考日期等状态标记
  - 后续参考日期调整
- 设备相关页面已拆成多个状态页，适合后续接入蓝牙或真实设备数据。
- 后端已完成五行测算、保存最近一次结果、读取最近一次结果。

### 当前更像“原型/演示”的部分

- 旧版登录流程说明已过时，当前以后端 session 和本地匿名浏览并存。
- 珠宝首页中的元素参考摘要来自本地保存结果，详情页与后端接口联动。
- 产品、设备、电量、推荐文案大多还是静态示例数据。
- `showcase`、`showcase-device` 更偏展示页面，是否保留要看后续产品方向。

## 4. 路由与分包

主包页面：

- `pages/home/index`
- `pages/login/index`
- `pages/legal/agreement/index`
- `pages/legal/privacy/index`

分包页面：

- `subpackage/jewelry/pages/data/index`
- `subpackage/jewelry/pages/settings/index`
- `subpackage/jewelry/pages/five-elements/index`
- `subpackage/periodCalendar/pages/calendar/index`

以下旧演示/旧认证分包已通过 `project.config.json` 打包忽略，当前不作为提审入口：

- `subpackage/showcase`
- `subpackage/showcase-device`
- `subpackage/auth`
- `subpackage/device12`
- `subpackage/device13`
- `subpackage/device17`

## 5. 后端接口说明

服务默认地址：

- `http://localhost:3000`

已实现接口：

### 1. 计算五行

- `POST /api/wuxing/calculate`

必传参数：

- `birthDate`，格式 `YYYY-MM-DD`
- `birthTime`，格式 `HH:mm`
- `gender`，取值 `male` 或 `female`

### 2. 保存最近一次五行结果

- `POST /api/wuxing/save`

说明：

- 会重新计算一次结果再保存
- `userId` 不传时默认使用 `default`

### 3. 获取最近一次结果

- `GET /api/wuxing/latest?userId=default`

### 4. 今日灵感

- `GET /api/inspirations/random`

## 6. 启动方式

### 前端

1. 用微信开发者工具打开目录：`D:\st\app\珠宝\新建文件夹`
2. 检查小程序 `AppID`、本地编译配置是否正确
3. 从主包首页进入，按页面路径联调各分包

### 后端

进入目录：

- `D:\st\app\珠宝\新建文件夹\backend`

启动命令：

```bash
npm install
npm run dev
```

## 7. 接手时建议先确认的事项

建议新同事先确认下面几件事：

1. 这个项目最终是要做“正式业务版”，还是“页面演示版/提案版”。
2. 五行色彩算法是否以当前本地 Node 服务为准，还是后续要切换成正式服务。
3. 登录、用户体系、结果存储，是否需要接入真实账号系统。
4. 设备页是否真的要接蓝牙能力，还是只做 UI 演示。
5. 周期日历功能是否属于同一个正式产品，还是临时合并展示。

## 8. 当前风险和注意点

- 前后端还没有看到完整闭环联调痕迹，后续需要重点确认请求接入点。
- 页面数量较多，但很多模块是展示态，接手时要先区分“已确认保留”和“仅供参考”的页面。
- 后端数据当前写本地 `json` 文件，只适合本地开发，不适合正式环境。
- 项目里设备、五行色彩、周期记录三条线并行，产品边界需要持续收敛，不然维护成本会越来越高。

## 9. 建议的下一步工作顺序

1. 先跑通前端和后端，确认接口可用、页面跳转正常。
2. 梳理哪些页面是真正要上线的，删除或归档纯展示页面。
3. 把五行定制页与后端接口正式联调，统一数据来源。
4. 明确设备模块是否接蓝牙，决定保留静态演示还是真实接入。
5. 再补登录、用户数据存储、接口异常处理等正式能力。

## 10. 交接结论

当前项目已经有比较完整的页面框架和一个可运行的五行后端，适合继续往“可演示”或“可上线雏形”推进。

如果是新同事接手，建议优先把下面三件事做清楚：

- 产品范围
- 真实保留页面
- 前后端联调方式

只要这三件事定下来，后续整理和推进会顺很多。
