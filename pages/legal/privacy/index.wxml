<view class="legal-page">
  <view class="legal-header" style="padding-top: {{headerTop}}px;">
    <text class="legal-title">{{copy.privacyTitle}}</text>
  </view>
  <scroll-view scroll-y="true" class="legal-scroll">
    <view class="legal-card">
      <text class="legal-heading">{{copy.privacyHeading}}</text>
      <text class="legal-meta">{{copy.meta}}</text>
      <block wx:for="{{copy.privacyParagraphs}}" wx:key="*this"><text class="legal-p">{{item}}</text></block>
    </view>
  </scroll-view>
  <view class="legal-footer">
    <button wx:if="{{consentMode}}" class="agree-btn" bindtap="acceptAndGoBack">{{copy.agreeAndBack}}</button>
    <button class="back-btn" bindtap="goBack">{{copy.back}}</button>
  </view>
</view>
