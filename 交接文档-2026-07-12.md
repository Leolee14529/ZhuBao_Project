# 珠宝小程序交接文档

> 交接日期：2026-07-12
> 当前工作分支：`codex/ui-records-typography-20260711`
> 仓库路径：`D:\st\app\珠宝\ZhuBao_Project`

## 1. 当前状态

- 项目已完成多轮前端 UI 与交互调整，重点集中在首页、数据记录页、设置页、睡眠详情页、底部导航、统一 Typography、顶部基准线与部分性能优化。
- 当前代码仍处于本地工作区，尚未完成对 GitHub 的最终推送。
- 已确认本次待提交范围中不包含 `.env`、证书、密钥、数据库账号等敏感文件。

## 2. 关键改动范围

### 首页

- 调整了「今日心情」「今日色彩」卡片的信息层级。
- 调整了「风格参考」模块标题与按钮的视觉主次。
- 优化了部分首页数据卡片的字号和排版。

### 数据记录页

- 新增「睡眠」与「今日活动」两张横向大卡片。
- 新增睡眠详情页，路径为 `subpackage/jewelry/pages/sleep-detail/index`。
- 调整了睡眠卡片、活动卡片、节奏记录、感受记录等模块的信息层级。

### 设置页

- 调整了设置页列表字号层级。
- 设置列表主文字、右侧状态、分组标题、删除账号按钮均做了统一 Typography 处理。

### 全局 Typography

- 新增全局字号系统文件 `styles/typography.wxss`。
- 所有页面最小字号已统一提升到 `20rpx` 以上。
- `row-label`、`health-title`、`nav-label` 等关键样式主要由全局 Typography 控制。

### 顶部与导航

- 新增 `utils/top-layout.js` 作为顶部安全区与内容基准线统一工具。
- 底部 TabBar 调整为文字优先的极简风格。
- 记录页与设置页底部导航统一到 `subpackage/jewelry/components/bottom-nav`。

## 3. 当前关键文件

- [styles/typography.wxss](D:/st/app/珠宝/ZhuBao_Project/styles/typography.wxss)
- [subpackage/jewelry/pages/data/index.wxml](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/data/index.wxml)
- [subpackage/jewelry/pages/data/index.wxss](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/data/index.wxss)
- [subpackage/jewelry/pages/settings/index.wxml](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/settings/index.wxml)
- [subpackage/jewelry/pages/settings/index.wxss](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/settings/index.wxss)
- [subpackage/jewelry/pages/sleep-detail/index.wxml](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/sleep-detail/index.wxml)
- [subpackage/jewelry/pages/sleep-detail/index.wxss](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/pages/sleep-detail/index.wxss)
- [subpackage/jewelry/components/bottom-nav/bottom-nav.wxml](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/components/bottom-nav/bottom-nav.wxml)
- [subpackage/jewelry/components/bottom-nav/bottom-nav.wxss](D:/st/app/珠宝/ZhuBao_Project/subpackage/jewelry/components/bottom-nav/bottom-nav.wxss)
- [utils/top-layout.js](D:/st/app/珠宝/ZhuBao_Project/utils/top-layout.js)

## 4. 已确认的样式覆盖关系

- 页面里改字号没生效的核心原因，是 `styles/typography.wxss` 中存在带 `!important` 的全局规则。
- 例如：
  - `.row-label`
  - `.health-title`
  - `.health-meta-value`
  - `.activity-value`
  - `.nav-label`
- 这意味着后续修改字号时，应该优先检查全局 Typography，再检查页面局部样式。

## 5. 当前未完成事项

- Git 提交身份尚未在当前环境配置，因此 `git commit` 之前还需要设置 `user.name` 和 `user.email`，或者使用仓库局部配置。
- 远程推送曾出现连接重置问题，需要在网络恢复后重新执行 `git push -u origin codex/ui-records-typography-20260711`。

## 6. 敏感文件检查结果

- 本次待提交清单中未包含 `.env`、私钥、证书、数据库配置文件。
- `project.config.json` 已判断为本地开发工具配置变更，不纳入这次提交。

## 7. 下一位接手建议

1. 先确认 Git 提交身份配置是否就绪。
2. 再核对当前暂存区是否仍符合预期。
3. 网络恢复后完成 `commit` 与 `push`。
4. 如继续做 UI 调整，优先改 `styles/typography.wxss`，不要先改页面局部 `wxss`。

