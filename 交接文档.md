# 珠宝项目交接文档

## 1. 当前版本

- 项目路径：`D:\st\app\珠宝\珠宝3\ZhuBao_Project-codex-project-upgrade-20260718`
- GitHub 仓库：`https://github.com/Leolee14529/ZhuBao_Project`
- 当前分支：`codex/20260723-syn`
- 最近提交：`a9f9186 Sync current project to GitHub branch`
- 分支地址：`https://github.com/Leolee14529/ZhuBao_Project/tree/codex/20260723-syn`
- 当前状态：已推送到远程，工作区无未提交修改

## 2. 项目结构

- 小程序主包：`pages/`、`components/`、`utils/`
- 珠宝业务分包：`subpackage/jewelry/`
- 经期日历分包：`subpackage/periodCalendar/`
- 设备相关分包：`subpackage/device12/`、`subpackage/device13/`、`subpackage/device17/`
- 后端：`backend/`
- 睡眠记录接口：`backend/routes/sleepRecords.js`
- 主要前端页面：
  - 首页：`pages/home/index.js`
  - 登录页：`pages/login/index.js`
  - 一级记录页：`subpackage/jewelry/pages/data/index.js`
  - 睡眠二级页：`subpackage/jewelry/pages/sleep-detail/index.js`
  - 经期日历页：`subpackage/periodCalendar/pages/calendar/index.js`

## 3. 已完成事项

### 记录系统

- 一级记录页保留睡眠、今日状态、感受记录和近 7 日节奏卡片。
- 睡眠记录使用独立 `sleep_records` 链路，一级页优先读取独立睡眠数据。
- `daily_checkins.sleepMinutes` 保留为历史兼容字段。
- 睡眠记录新增、编辑、日期查询和趋势数据链路已保留。
- 一级页卡片采用独立加载和错误处理，睡眠失败不应阻塞其他卡片。

### 睡眠系统

- 前端入口：`utils/sleep-records.js`。
- 睡眠二级页：`subpackage/jewelry/pages/sleep-detail/`。
- 主要接口：`/api/sleep-records`。
- 独立睡眠数据字段：`recordDate`、`sleepDurationMinutes`、`sleepHours`、`sleepMinutes`。
- 趋势柱形高度和文字均使用 `sleepDurationMinutes`。
- 空分钟保存时按 `0` 处理，并在保存时校验总时长范围。

### 经期系统

- 经期日历使用本地周期配置和主包共享工具。
- 经期工具已放置在主包 `utils/`，避免珠宝分包直接跨分包引用：
  - `utils/cycle-engine.js`
  - `utils/cycle-summary.js`
  - `utils/date-utils.js`
- 一级记录页和经期日历页共用同一套周期计算逻辑。

### 商品内容

- 首页风格参考图片路径已补齐并使用项目内静态资源。
- “墨翠层叠胸链”已修正为“墨翠层叠戒”。
- “绿晶耳挂”已修正为“芙蓉石项链《南海》”，分类和相关描述同步调整。
- 商品详情页底部“记录今日感受”入口已移除。

### 黑屏问题

已确认的主要黑屏原因是记录页存在跨分包引用：

```js
require("../../../periodCalendar/utils/cycle-engine")
```

微信开发者工具报错：

```text
module 'subpackage/periodCalendar/utils/cycle-engine.js' is not defined
```

现已改为引用主包 `utils/` 中的共享工具。若再次黑屏，优先查看开发者工具 Console 的第一条红色错误，不要先改 UI 或后端。

## 4. 开发环境

- Node.js：`D:\app\Nodejs\node.exe`
- 当前版本：`v24.18.0`
- npm：`11.16.0`
- 后端目录：`backend/`
- 后端启动文件：`backend/server.js`
- 默认端口：`3000`
- 推荐监听地址：`0.0.0.0`
- 开发 API 地址：`http://127.0.0.1:3000` 或按当前开发者工具网络条件使用 `http://192.168.3.60:3000`

启动后端：

```powershell
Set-Location "D:\st\app\珠宝\珠宝3\ZhuBao_Project-codex-project-upgrade-20260718\backend"
& "D:\app\Nodejs\node.exe" .\server.js
```

健康检查：

```powershell
Invoke-RestMethod "http://127.0.0.1:3000/health/ready"
```

如果使用局域网地址测试：

```powershell
Invoke-RestMethod "http://192.168.3.60:3000/health/ready"
```

微信开发者工具本地 HTTP 调试时，需要勾选“不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书”。正式环境不要关闭安全校验。

## 5. 数据和身份规则

- 综合记录主表：`daily_checkins`。
- 独立睡眠主表：`sleep_records`。
- 睡眠新增和更新只写 `sleep_records`。
- 不要使用固定睡眠账号、固定密码或固定用户 ID。
- 后端用户身份应以 `Authorization: Bearer <token>` 解析出的 `req.user.id` 为准。
- `x-app-user-id` 不得覆盖 token 对应用户身份。
- JSON 回退数据必须按真实 `userId` 分桶读取。
- 不要把没有用户 ID 的历史测试记录自动归属给新用户。

当前后端没有 `DATABASE_URL` 时，睡眠记录可能使用 JSON 回退存储。不要直接清空 JSON 文件；排查串数据时先确认当前请求用户 ID 和记录所属用户 ID。

## 6. 重要排查方法

### 记录页黑屏

1. 查看 Console 第一条红色错误。
2. 确认 Sources 加载的是当前项目目录。
3. 检查 `subpackage/jewelry/pages/data/index.js` 的 `require` 路径。
4. 检查 `index.json` 的组件注册和 `index.wxml` 是否为完整版本。
5. 不要用全屏黑色 loading 掩盖异常。

### 登录超时

1. 先访问 `/health/ready`。
2. 确认微信登录请求的最终 URL。
3. 确认后端终端是否出现请求入口日志。
4. 若后端没有收到请求，排查开发者工具网络、IP、代理和防火墙。
5. 若收到请求，再排查 `wechat-login` 内部的 `code2Session`、用户查询和响应。

### 新用户读到旧数据

1. 只打印 token 前 10 位，不打印完整 token。
2. 对比前端 `userInfo.id`、后端 `req.user.id` 和睡眠请求用户 ID。
3. 检查 `daily_checkins` 和 `sleep_records` 是否按用户过滤。
4. 检查退出登录是否清理 token、用户信息和页面内存状态。
5. 不要通过隐藏卡片或清空数据库伪装成无数据。

## 7. 建议回归测试

- 登录成功、账号登录失败时快速返回错误。
- 首页风格参考 5 张图片首次打开正常显示。
- 一级记录页首次进入不黑屏，标题、卡片和底部导航完整。
- 睡眠二级页能读取有记录和无记录日期。
- 小时 7、分钟为空时保存为 420 分钟。
- 修改睡眠记录不会新增重复记录。
- 返回一级页后睡眠卡片显示最新时长。
- 经期卡片可进入日历页，返回后摘要刷新。
- 新用户看不到其他用户的 daily 或 sleep 数据。
- 连续切换首页、记录、设置页面 5 次不黑屏。
- 商品首页卡片与详情页名称、分类、图片对应一致。

## 8. 交接注意事项

- 当前分支是本次交接基线，不要直接在默认分支上强推。
- 修改前先查看工作区状态并保留用户已有改动。
- 不要随意修改全局 `utils/config.js`、`utils/request.js`，公共请求层改动会影响登录和全部记录卡片。
- 不要同时大范围修改前端、后端和数据库。
- 后续修复应先获取真实日志，再做最小范围改动。
- `.gitignore` 已排除环境变量、运行时用户数据、会话数据、依赖目录和日志；不要把这些敏感或临时文件强行加入 Git。

