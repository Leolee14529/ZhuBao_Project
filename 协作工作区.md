# ZhuBao 并发协作工作区

更新时间：2026-07-11（Asia/Shanghai）

## 1. 当前共享基线

- 仓库：`C:\Users\Administrator\Documents\ZhuBao\ZhuBao_Project`
- 分支：`codex/share-audit-ui-polish`
- 协作开始时 HEAD：`10ff70926dc5bfbd11277308bf2d24bc4c1ad581`
- 两个线程共享同一个工作区，必须串行提交，不得同时暂存或提交。

## 2. 通用规则

1. 不使用 `git add .`、`git add -A`、`git stash`、`git reset --hard` 或 `git checkout --`。
2. 每个线程只暂存本节分配给自己的文件，并在提交前再次执行 `git diff --cached --name-only`。
3. 不覆盖、不格式化、不回滚另一个线程的未提交内容。
4. 需要修改对方负责的文件时，必须等对方提交后重新读取最新文件，再做小范围补丁。
5. 每次提交后在本文档登记 commit、文件范围、验证结果和遗留问题。
6. `协作工作区.md` 由线程 B 维护；线程 A 只需读取并在交接时提供 commit 信息。

## 3. 线程 A：心情、首页与后端功能

当前优先级：先完成并提交。

负责范围：

- `backend/README.md`
- `backend/app.js`
- `backend/routes/users.js`
- `backend/routes/mood.js`
- `backend/repositories/moodRepository.js`
- `backend/services/mood*.js`
- `backend/db/migrations/004_daily_mood.sql`
- `backend/test/api.test.js`
- `backend/test/compliance-home.test.js`
- `backend/test/mood-service.test.js`
- `pages/home/index.js`
- `pages/home/index.wxml`
- `pages/home/index.wxss`
- `pages/home/mood.wxss`
- `pages/home/mood-runtime.js`
- `pages/legal/agreement/index.wxml`
- `pages/legal/privacy/index.wxml`
- `utils/auth.js`
- `utils/request.js`
- `.gitignore`（仅用于忽略 `backend/data/moods.json`）
- `backend/test/auth-local-state.test.js`（仅用于失效会话/guest ID 回归）

交接要求：

- 提交完成后登记 commit hash。
- 列出实际提交文件和测试结果。
- 说明是否改变页面初始 data、登录状态、路由或首页组件尺寸。

## 4. 线程 B：页面闪屏与组件首帧稳定

当前状态：线程 A 两个提交均已完成准入；线程 B 正在基于 `c6cdbe4` 实施闪屏修复。

负责范围：

- `app.json`
- `components/status-bar/**`
- `pages/login/index.json`
- `pages/login/index.wxss`
- `pages/legal/agreement/index.json`
- `pages/legal/privacy/index.json`
- `pages/home/nav.wxss`
- `pages/home/index.js`、`pages/home/index.wxss`（仅在 A 提交后修复首帧 topSpacer 与心情卡稳定高度）
- `subpackage/jewelry/components/bottom-nav/**`
- `subpackage/jewelry/utils/page-layout.js`
- `subpackage/jewelry/pages/data/index.js`
- `subpackage/jewelry/pages/products/**`
- `subpackage/jewelry/pages/product-detail/**`
- `subpackage/jewelry/pages/five-elements/**`
- `subpackage/jewelry/pages/settings/index.js`
- `subpackage/jewelry/pages/settings/index.wxss`
- `subpackage/periodCalendar/pages/calendar/**`
- `subpackage/periodCalendar/components/cycle-setup-sheet/**`
- `subpackage/periodCalendar/components/reference-switch/**`

线程 B 不并发修改线程 A 文件。若提交审查证明必须调整 `utils/auth.js` 或首页文件，等线程 A 提交后单独做最小补丁并在本文档登记。

## 5. 串行提交顺序

1. 线程 A 完成代码、测试并提交。
2. 线程 B 检测 HEAD 变化，并连续两次读取到相同 HEAD 和相同工作区文件清单后，审查线程 A 的 commit diff。
3. 线程 B 在微信开发者工具中验证：首页可读取、无 AppService 异常、登录态与匿名态路由正常、首页组件尺寸稳定。
4. 线程 B 基于新的 HEAD 实施闪屏修复。
5. 线程 B 完成静态检查、10 个注册路由回归、底部导航切换和连续帧检测后，仅暂存自己的文件并提交。

## 6. 提交登记

| 顺序 | 线程 | Commit | 文件范围 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | A：心情/首页/后端 | `a65dc06` | 21 个功能文件 | syntax、lines、61/61 tests | 已提交，准入审查发现阻断项 |
| 2 | A：隐私/owner 准入修复 | `c6cdbe4` | 5 个约定文件 | Node 24 syntax、lines、67/67 tests、`git show --check` | 已提交并通过 B 准入 |
| 3 | B：闪屏/首帧 | `2448fcc` | 33 个首帧、导航、商品与日历文件 + 本文档 | Node 24、DevTools、10 路由、连续帧 | 已验证并提交 |

线程 A 交接时需要登记：提交 SHA、提交标题、实际提交文件、仍保留的未提交文件、验证命令与结果、已知风险和交接时间。

提交前基线（线程 A 未提交状态）：

- 125 个 JS 文件语法检查通过。
- 68 个 JSON 文件解析通过。
- 后端 61 个测试通过，0 失败。
- DevTools 当前首页可读取，`.home-page` 与 `.mood-card` 尺寸有效，未捕获新的 `console.error` 或 `exception`。
- 阻断提醒：`pages/home/index.js` 已引用 `./mood-runtime`；线程 A 必须把当前未跟踪的 `pages/home/mood-runtime.js` 一并提交，否则干净工作区首页会出现模块无法读取。

`a65dc06` 准入审查：

- 文件完整、静态检查通过、DevTools 10 个注册路由均可读取，无新增 `console.error` / `exception`。
- 阻断：401 guest 回退在清理 owner-scoped 本地个人数据前删除 userInfo，原账号的生日/周期等键可能遗留。
- 阻断：匿名首页在未确认隐私同意时创建并发送持久 guest ID。
- 阻断：`.gitignore` 未忽略运行时 `backend/data/moods.json`。
- 非阻断：晚到 mood 响应可能在页面卸载或身份切换后写入错误 owner 缓存。
- 线程 A 需用独立 follow-up commit 修复以上项目；线程 B 继续等待，不夹带代修。

`c6cdbe4` follow-up 准入审查：

- 提交范围严格为 `.gitignore`、`backend/test/auth-local-state.test.js`、`pages/home/index.js`、`pages/home/mood-runtime.js`、`utils/auth.js`。
- `git show --check` 通过；Node 24 下语法、行数和 67/67 测试通过。
- 原 owner 清理、隐私同意前 guest ID、损坏 guest ID 自愈和晚到响应 owner 保护均有回归覆盖。
- 提交后工作树稳定，线程 B 才开始写入自己的文件。

## 7. 线程 B 对上一提交的准入审查

- `git show --check <A_SHA>`
- `git show --stat --oneline <A_SHA>`
- `git diff <A_SHA>^ <A_SHA> --name-status`
- `git cat-file -e <A_SHA>:pages/home/mood-runtime.js`
- 使用项目指定的 Node 24 运行后端语法检查和测试。
- 解析全部小程序 JSON，并对相关 JS 执行语法检查。
- 在微信开发者工具中打开首页和全部注册路由，监听 `console.error` 与 `exception`。
- 若上一提交造成编译失败、页面无法读取或路由阻断，先记录为线程 A 阻断项，不把代修混入线程 B 的闪屏提交。

## 8. 线程 B 回归闸门

- 所有 JS 语法检查通过，所有页面 JSON 可解析。
- 微信开发者工具能读取当前项目，且没有编译错误或阻塞对话框。
- 10 个注册路由的实际路径、根选择器和关键内容选择器存在。
- AppService 没有新增 `exception` 或 `console.error`。
- 首页、记录、设置的底部导航切换不重复触发。
- 商品详情和周期页首帧不是透明或空内容壳。
- 连续帧检测没有白帧；截图失败与功能断言分开报告。

最终结果：

- 微信开发者工具 CLI `preview` 成功，总包约 1.4 MB。
- 10 个注册路由均能读取，根节点尺寸有效且为深色；AppService 新增错误与异常为 0。
- 首页心情卡固定为 300rpx 并允许三行描述，商品详情首帧有稳定暗色壳，周期页首帧已有 6 周数据。
- 底部导航连续双击只产生一次跳转，页面栈保持 1；周期设置弹层首次打开和关闭后重开均为 6 周完整状态。
- 6 组关键页面切换共采样 47 帧，白帧 0、截图失败 0。
- Node 24：JS 语法、67/67 测试、300 行限制通过；86 个 JSON 文件解析通过；`git diff --check` 通过。

## 9. GitHub 发布交接

- 发布版本：`1.1.0`。
- 新分支：`agent/miniapp-stability-v1.1.0`。
- PR base：`codex/share-audit-ui-polish`，用于把评审范围筛选为本轮 3 个功能提交及版本文档提交。
- 远端默认分支 `sync/local-to-github` 是当前历史的祖先；若直接以默认分支做 PR base，会额外显示 10 个尚未合并的历史提交。
- 本地旧 `stash@{0}` 涉及大量历史文件，本次没有 apply、pop 或纳入提交。
- GitHub 保留 README、协作和交接文档；微信上传包通过 `project.config.json` 排除这些内部文档。
